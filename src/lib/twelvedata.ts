// Stub — Phase 1 fills. TwelveData free: 8 calls/min, 800/day. Covers XAUUSD, NQ, DXY.
export async function getPrice(symbol: string) { return null; }
export async function getCandles(symbol: string, interval: string, count: number) { return []; }
