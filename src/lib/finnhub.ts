// Stub — Phase 2 fills. Finnhub free: 60 calls/min. Covers QQQ (NQ proxy), SPY (ES proxy), VIX, news, econ calendar.
export async function getNews(category?: string) { return []; }
export async function getEconomicCalendar(from: string, to: string) { return []; }
export async function getQuote(symbol: string) { return null; }
